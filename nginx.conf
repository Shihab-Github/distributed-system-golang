events {}

http {
    upstream streamhub_backend {
        server host.docker.internal:8080;
        server host.docker.internal:8081;
    }

    server {
        listen 8090;

        location / {
            proxy_pass http://streamhub_backend;
        }
    }
}